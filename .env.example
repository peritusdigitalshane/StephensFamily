# Database
DATABASE_URL="file:./dev.db"

# NextAuth - IMPORTANT: Generate a real secret for production
# Run: openssl rand -base64 32
NEXTAUTH_SECRET=change-me-to-a-random-secret
NEXTAUTH_URL=http://localhost:3000
